import { formHandler } from "./js/formHandler"

import './styles/style.scss'

// main function that handles the form inputs and when the 2 buttons are clicked
document.addEventListener('DOMContentLoaded', () => {
    formHandler();
});